# branching_model — release-candidate branches strategy

A reference repository illustrating a **release-candidate (RC)** workflow,
where releases are stabilized through `*-rcn` tags in a release-candidate
branch before the final tag.

![Release-candidate branching model](rc_branches.svg)
